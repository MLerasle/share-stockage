%section#main-content
	.row.body.sign-in
		.large-7.small-12.columns.large-centered.small-centered
			.create-account
				.row.sign-up
					.large-12.small-12.columns
						%h1.subheader Mot de passe oublié?
						%hr
						%h6.subheader Veuillez entrer votre adresse email, nous vous enverrons les instructions pour changer votre mot de passe.
						%br
						= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
							= f.email_field :email, autofocus: true
							= f.submit "Valider", class: "button radius success"